<?xml version="1.0" encoding="utf-8"?>
<!--
  Android blocks cleartext (plain http://) traffic by default on API 28+.
  The app's default BACKEND_BASE is http://localhost:8000, and a real device or
  emulator reaches a dev backend over plain HTTP, so we must explicitly allow
  cleartext to the local dev hosts below. Everything else still requires HTTPS,
  so a deployed (https://) backend needs no change here.

  Reach your dev backend with a matching dart-define (BACKEND_BASE=...):
    - Android emulator: http://10.0.2.2:8000   (10.0.2.2 = your host machine)
    - Physical device:  http://[your-LAN-IP]:8000  (add that IP below)
    - adb reverse:       http://localhost:8000  (after: adb reverse tcp:8000 tcp:8000)
-->
<network-security-config>
    <!-- Secure by default: every domain not listed below must use HTTPS. -->
    <base-config cleartextTrafficPermitted="false" />

    <!-- Plain-HTTP allowed ONLY for local development backends. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- Testing on a physical device? Add your dev machine's LAN IP, e.g.:
        <domain includeSubdomains="false">192.168.1.50</domain> -->
    </domain-config>
</network-security-config>
