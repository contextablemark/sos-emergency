# NOTE: the Gradle wrapper (gradlew, gradlew.bat, gradle-wrapper.jar) is
# intentionally tracked so the project is buildable straight after a git clone /
# branch pull, without regenerating the wrapper. (Flutter's default template
# ignores these.)
/.gradle
/captures/
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
